<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<VBox xmlns="http://javafx.com/javafx"
      xmlns:fx="http://javafx.com/fxml"
      fx:controller="com.salledesport.controller.SeanceController"
      spacing="20" style="-fx-padding: 25; -fx-background-color: #f8f9fa;">

    <!-- En-tête -->
    <HBox alignment="CENTER_LEFT" spacing="15">
        <Label text="📅 Gestion des Séances" style="-fx-font-size: 28px; -fx-font-weight: bold; -fx-text-fill: #2c3e50;"/>
        <Region HBox.hgrow="ALWAYS"/>
        <Label fx:id="totalSeancesLabel" text="Total: 0 séance(s)"
               style="-fx-font-size: 14px; -fx-text-fill: #6c757d;"/>
    </HBox>

    <Separator/>

    <!-- Barre de recherche et actions -->
    <HBox spacing="15" alignment="CENTER_LEFT">
        <TextField fx:id="rechercheField"
                   promptText="🔍 Rechercher par type ou date..."
                   prefWidth="400"
                   style="-fx-font-size: 14px; -fx-padding: 10;"/>

        <Button text="Rechercher" onAction="#handleRechercher"
                style="-fx-background-color: #007bff; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 10 20; -fx-cursor: hand;"/>

        <Button text="Réinitialiser" onAction="#handleReinitialiser"
                style="-fx-background-color: #6c757d; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 10 20; -fx-cursor: hand;"/>

        <Region HBox.hgrow="ALWAYS"/>

        <Button text="➕ Nouvelle Séance" onAction="#handleNouvelleSeance"
                style="-fx-background-color: #28a745; -fx-text-fill: white; -fx-font-size: 14px; -fx-font-weight: bold; -fx-padding: 10 25; -fx-cursor: hand;"/>
    </HBox>

    <!-- TableView -->
    <TableView fx:id="seanceTable" VBox.vgrow="ALWAYS"
               style="-fx-background-color: white; -fx-border-color: #dee2e6; -fx-border-radius: 5;">
        <columns>
            <TableColumn fx:id="idColumn" text="ID" prefWidth="60"/>
            <TableColumn fx:id="dateColumn" text="Date" prefWidth="120"/>
            <TableColumn fx:id="heureColumn" text="Heure" prefWidth="100"/>
            <TableColumn fx:id="typeColumn" text="Type" prefWidth="150"/>
            <TableColumn fx:id="coachColumn" text="Coach" prefWidth="200"/>
            <TableColumn fx:id="membreColumn" text="Membre" prefWidth="200"/>
        </columns>
    </TableView>

    <!-- Boutons d'action -->
    <HBox spacing="15" alignment="CENTER_LEFT">
        <Button text="✏️ Modifier" onAction="#handleModifier"
                style="-fx-background-color: #ffc107; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 10 25; -fx-cursor: hand;"/>

        <Button text="🗑️ Supprimer" onAction="#handleSupprimer"
                style="-fx-background-color: #dc3545; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 10 25; -fx-cursor: hand;"/>

        <Region HBox.hgrow="ALWAYS"/>

        <Label fx:id="selectionLabel" text="Aucune sélection"
               style="-fx-text-fill: #6c757d; -fx-font-style: italic;"/>
    </HBox>

    <!-- Formulaire (caché par défaut) -->
    <VBox fx:id="formulaireBox" spacing="15"
          style="-fx-background-color: white; -fx-padding: 25; -fx-border-color: #007bff; -fx-border-width: 2; -fx-border-radius: 5;"
          visible="false" managed="false">

        <HBox alignment="CENTER_LEFT">
            <Label fx:id="formulaireTitreLabel" text="➕ Nouvelle Séance"
                   style="-fx-font-size: 20px; -fx-font-weight: bold; -fx-text-fill: #007bff;"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Button text="✕" onAction="#handleAnnuler"
                    style="-fx-background-color: transparent; -fx-text-fill: #dc3545; -fx-font-size: 20px; -fx-cursor: hand;"/>
        </HBox>

        <Separator/>

        <!-- Ligne 1 : Date et Heure -->
        <HBox spacing="15">
            <VBox spacing="8" HBox.hgrow="ALWAYS">
                <Label text="Date *" style="-fx-font-weight: bold; -fx-text-fill: #495057;"/>
                <DatePicker fx:id="datePicker" promptText="Sélectionnez une date"
                            prefWidth="250"
                            style="-fx-font-size: 14px;"/>
            </VBox>

            <VBox spacing="8">
                <Label text="Heure *" style="-fx-font-weight: bold; -fx-text-fill: #495057;"/>
                <HBox spacing="10" alignment="CENTER_LEFT">
                    <Spinner fx:id="heureSpinner" prefWidth="80" editable="true"/>
                    <Label text=":" style="-fx-font-size: 18px; -fx-font-weight: bold;"/>
                    <Spinner fx:id="minuteSpinner" prefWidth="80" editable="true"/>
                </HBox>
            </VBox>
        </HBox>

        <!-- Ligne 2 : Type de séance -->
        <VBox spacing="8">
            <Label text="Type de séance *" style="-fx-font-weight: bold; -fx-text-fill: #495057;"/>
            <ComboBox fx:id="typeComboBox" promptText="Sélectionnez le type"
                      prefWidth="400"
                      style="-fx-font-size: 14px;"/>
        </VBox>

        <!-- Ligne 3 : Coach et Membre -->
        <HBox spacing="15">
            <VBox spacing="8" HBox.hgrow="ALWAYS">
                <Label text="Coach *" style="-fx-font-weight: bold; -fx-text-fill: #495057;"/>
                <ComboBox fx:id="coachComboBox" promptText="Sélectionnez un coach"
                          prefWidth="300"
                          style="-fx-font-size: 14px;"/>
            </VBox>

            <VBox spacing="8" HBox.hgrow="ALWAYS">
                <Label text="Membre *" style="-fx-font-weight: bold; -fx-text-fill: #495057;"/>
                <ComboBox fx:id="membreComboBox" promptText="Sélectionnez un membre"
                          prefWidth="300"
                          style="-fx-font-size: 14px;"/>
            </VBox>
        </HBox>

        <!-- Note champs obligatoires -->
        <Label text="* Champs obligatoires"
               style="-fx-text-fill: #dc3545; -fx-font-size: 12px; -fx-font-style: italic;"/>

        <!-- Boutons formulaire -->
        <HBox spacing="15" alignment="CENTER">
            <Button text="💾 Enregistrer" onAction="#handleEnregistrer"
                    style="-fx-background-color: #28a745; -fx-text-fill: white; -fx-font-size: 15px; -fx-font-weight: bold; -fx-padding: 12 40; -fx-cursor: hand;"/>

            <Button text="❌ Annuler" onAction="#handleAnnuler"
                    style="-fx-background-color: #6c757d; -fx-text-fill: white; -fx-font-size: 15px; -fx-padding: 12 40; -fx-cursor: hand;"/>
        </HBox>
    </VBox>

</VBox>
